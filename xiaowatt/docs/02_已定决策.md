# 02 · 已定决策（甲方已拍板或已通过验证，不要推翻）

## 产品形态
1. **本届展示场景＝倒闸操作**（曾有"组网"口径，已作废）。学员扮演操作人；监护人视角在入口保留但标注"下一版本开放"。
2. **对话才是中心，设备只是道具**：陪练舱 v2 布局＝左 44% 数字人 + 右 56% 对话气泡流；作业面板是底部抽屉（Sheet），需要手指/执行/回报的节拍自动升起，标√后收回；抽屉打开时数字人自动缩小不被遮挡。
3. **教学引导性是硬要求**：三种模式（教学/演练/考核）、常驻任务指令条（分步打勾）、知识点卡（仅新主题自动展开）、三级提示（留痕扣分不阻断）、九主题知识地图、阶段预习卡、首次七步导览、教学模式下监护人带路（自动切位置）+ 同类错误首次只提醒不判违规。
4. **五拍闭环**是产品的业务灵魂：唱票 → 手指口述 → 对，执行 → 执行 → 检查回报 → 标√。缺任何一拍都要有判定。
5. 六处红线已埋并回归验证：未验电合地刀（一票否决+Canvas 弧光事故推演五段后果链）、异常未中止、跳项、走错间隔、GIS 只看后台、第 9 项票令陷阱（调度故意念错）。异常处置支线：中止→汇报值班负责人→逐级上报→研判→恢复。
6. 八种练习方式：完整票 / 三段分段 / 专项（验电接地、GIS 四项核对、接令与票令核对含陷阱）/ 错题重练（localStorage xwt_lastvio）。分段与专项会预置设备状态。
7. 评分＝六维雷达+否决项+加分项+能力标签+生成式复盘（genReview 引用本场真实数据）；完整成长曲线、学习地图、岗位胜任度放「评分复盘/成长档案」页做。
8. 底座证据页清单与建设顺序：**工作台 Dashboard → AI 教练中心 → 评分复盘 → 成长档案 → 教练编辑器（重点演示"从操作票一键生成剧本"）→ 班组看板 → 知识课堂接入**。
9. 「场景商店」这个名字不用，改为体现"很多可选的智能陪练教练"（AI 教练中心），预设大量不同类型教练以显底座丰富完整。

## 数字人
10. 数字人**改用 HeyGen**，背景**透明通道 WebM**；渲染由用户自行在 HeyGen 侧执行（形象用 AI 生图，提示词已交付：中年男监护人为主角+女调度员+男值班负责人）。
11. 播放层三档已定：clips（预渲染，主用，离线）/ stream（实时，联网自由问答）/ builtin（SVG 兜底）；任何环境不出空画面。
12. 旧 WebM 端点不支持自定义形象 → heygen-kit.js 走 v3（output_format:webm）优先、自动回退 v2（background:transparent）；形象需带抠像（matting）训练，`node heygen-kit.js looks` 可查。
13. 内置 SVG 数字人的口型＝拼音→十种视位、每字两帧（声母闭合/韵母开口）与语音逐字对齐；这套「同源驱动」指示条保留，是技术展示点。

## 工程
14. 单文件离线 HTML，双击即开；构建＝build.py 拼接；测试＝playwright 驱动 autoStep 全流程回归（关埋点跑通零违规、开埋点准确停在陷阱/异常）。
15. submitInput 有并发锁（S.lock）、tickStep 有幂等保护——修改交互时不要绕开。
16. 知识问答（问教练）＝对 KNOW 九主题的二元组检索加权（主题名 ×3 / 小节名 ×2 / 正文 ×1 / 首节 +2 / 当前项关联 ×1.25），答案必须带依据出处。
17. 中文文件名的 HTML 未再出现下载问题，交付物用中文名。

## 语言与表述（全局）
18. 材料包语言规则：只写"需求文档"不用英文缩写；不写空泛表述；人员评价类用脱敏模拟数据、结论由人确认。
19. 用词：不用"手术/骨架/刀"、不用"门禁"、标题正向表述；面向客户高层的文案不用俚俗口语腔。
20. 原型（含新页面）不得出现介绍式/讲解员式内容——与 CLAUDE.md 铁律 1 同义，这是最高频被纠正的一条。

## 2026-09-01 增补（甲方新口径，覆盖此前相关条目）
21. **底座各页浅色「白+金+绿」主题**（白底、金 #c9a227、绿 #0e8f5a，图表与菜单同色系）；陪练舱保持深色现场风。
22. **去掉浏览器 TTS 语音播报**（太生硬）：内置数字人只保留字幕+口型；真人声由 HeyGen 预渲染片段承担。
23. **陪练交互不允许"只点鼠标"**：演练/考核模式下手指口述与执行为**长按**（带进度环），教学模式点按；任务条升级为**指令卡**（动作图标+做什么+怎么做+「我该做什么」+「前往」）；目标设备高亮脉冲；卡住 24 秒监护人主动开口提醒（考核模式除外）。
24. **每位预设教练配虚拟形象图**：图片放 assets/coaches/<id>.png 构建时内联；18 条生成提示词见 docs/教练形象_生成提示词.md。
25. **工作台为驾驶舱布局**：中央「学员成长地图」（成长跑道 + 三阶段分区 + 流向边与流动粒子 + 状态节点，全节点可下钻至明细/陪练/课堂），六类图表环绕（雷达/环形/面积/热力/双轴柱线/仪表盘），不再用整齐宫格。
26. **全站浅色**（覆盖 21 条中"陪练舱保持深色"）：陪练舱同样浅色白+金+绿；设备语义色（带电红/停电绿/接地黄/挂牌）保留。**三阶段强指引**：上岗前准备、五防模拟、操作票执行全程指令卡逐项指路（做什么+怎么做+动作示意图动画），当前目标元素金色脉冲高亮，错点提示指明正确的下一步；数字人全程随阶段开口（视频层为 HeyGen 片段，到货接入）。
27. **角色切换与管理端**：右上角账号可在学员（任玲玲）与班组长（周建国）间切换；班组看板/教练编辑器仅班组长可进，学员点击给权限提示。**底座五页全部为"有价值可讲"的完整页**：评分复盘（逐句回放 diff、错误卡、AI 复盘、错题重练直达）、成长档案（学习地图、晋升通道、里程碑）、知识课堂（接入关系图、错题↔题库联动）、班组看板（成员/热力/红线/提醒/任务下发闭环）、教练编辑器（一张操作票 → 逐条解析 → 校核 → 发布为自建教练，教练中心可见）。

28. **陪练舱数字人改为"形象版"**（9/2 甲方：骨骼数字人太丑不可接受）：以 18 位教练形象图中的三位为本体（监护人陈志远=倒闸操作形象、值班调度员林岚=调度术语形象、值班负责人周建国=安规考问形象），用呼吸起伏、说话节奏、声波、光环、视线跟随、姿态前倾/点头/摇头让其"动起来"；口型仍由逐字视位时钟驱动（同步条显示"正在说话 · 当前字 · 进度"），真人声由 HeyGen 片段承担。`DH.speak/setPose/nod/shake/speaking` 接口不变，回归脚本不改。
29. **成长档案用十维**（DIMS10 = 六维 + 操作顺序/验电接地/二次隔离/记录规范），前六维与陪练舱报告、首页、班组看板的六维同值同序；成长曲线为多序列（得分、7 日均线、用时、扣分项、提示次数、及格/考核线）可切换；目标由本人填写、预测为线性外推，均标注"以人工审核为准"。
30. **知识课堂与编辑器增加可操作功能**：课堂 = 课程库（搜索/标签/章节学习 → 学时回写）+ 随堂测验（题库 14 题，依据只引用既有条款）+ 本周学习计划（生成 → 本人确认加入日程）+ 接口节点下钻 + 立即同步；编辑器 = 18 形象 + 数字人预览试听 + 开场白生成 + 步骤上移/下移/红线/删除 + 一致性检查 + 剧本试演 + 评分模板与示例试算 + 知识库检索测试与文本上传 + 已发布列表。所有本机操作落 localStorage，不依赖外网。
31. **陪练舱新增五项实时反馈**：预估得分 KPI、本项用时/参考、复诵实时吻合度与漏说要素（按模式分级：教学给要素、演练给数量、考核不给）、目标设备 AR 标注、监护人逐项点评（考核模式不点评）。均为叠加层，不改动五拍闭环与判定逻辑。
32. **配色体系第三版（9/2 甲方）**：① 所有卡片金色描边并带流转光带；② 绿系与橙系作为两套主色，按卡片分配、绝不同卡混用（绿=AI/能力/过程，橙=人力资源/任务/人员），通过 `.hg/.ho` 类与 `--ac` 令牌族实现，图表随卡片色系切换；③ AI 时尚元素（流光线、渐变标题、AI 脉冲标记、数字滚动、粒子）与人力资源元素（班组伙伴头像条、上升人形、成长路径）均以动效呈现。语义色（带电红/停电绿/接地黄/红线红）不参与分卡规则。

