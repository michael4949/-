# 交接说明（9/21，换 Claude Code 会话继续）

> 新会话第一件事：读 `CLAUDE.md`（工作区总纲、铁律、代码地图、命令），再读本文件，然后按 `docs/03_任务清单.md` 末尾的"待办"干活。**距 9/23 成果交付与模拟答辩只剩两天。**

## 1. 代码在哪、怎么接着跑

- 仓库 `michael4949/-`，分支 **`claude/attachment-analysis-6pz04l`**，工作区 `xiaowatt/`。最后一次提交 `931f207`，已推送，工作树干净。
- 新会话启动：`git fetch origin claude/attachment-analysis-6pz04l && git checkout claude/attachment-analysis-6pz04l`。
- 提交身份沿用 `-c user.name="xiaowatt" -c user.email="luma8888666@gmail.com"`；不建 PR，只推本分支。
- 项目一构建与回归（需 `npm i playwright && npx playwright install chromium`，在 `peilian/` 下执行）：

```bash
cd peilian && python3 build.py        # → dist/小瓦特练_AI智能陪练底座_高保真原型.html
node v2base.js        # 底座八页冒烟，期望 ERR none
node v2exam1163.js    # 1163 关卡，期望 score 15 / 14.7 / 红线 0 / miss / listed 11 / ERR none
node v2examrain.js    # 雨淋阀关卡，期望 13.5 / 12.2 / boxOpen true / handle 100 / ERR none
```

- 项目二：`cd banzu && python3 build.py && node smoke.cjs`，期望 FAILS 0 ERR none（本轮未动）。

## 2. 项目一现在是什么样（9/13 一天之内六轮修改后的形态）

产品＝**底座 + 陪练关卡**，陪练舱模块已整体删除（docs/02 第 49 条）。

| 环节 | 现状 |
|---|---|
| 首页 | 偏人力资源：作业授权认证进度、待考任务、8 维雷达、成长地图、关卡分布 / 成绩 / 用时、能力对标、胜任度、AI 推荐、最近复盘、我在练的教练；数据全部由近30天关卡记录推导（本机记录 + 7 条模拟 `HIST_SEED`） |
| 教练中心 | 18 位教练目录，进入教练＝进入其陪练关卡（daozha → 1163，fire → 雨淋阀），其余只能申请开通 |
| 陪练关卡 | 做题 + 操作交替：选择 / 填空 / 问答在教练对话里一问一答即时回应（训练 / 演练答错可再答一次记半分，考核一次作答）；操作关屏幕上先给"这一关怎么做"（训练给做法、演练给要点、考核只给任务），做完当场打分。1163 十五分制、雨淋阀 13.5 分制。图示只画与本关操作相关的部件。数字人教练在右栏，朗读语音默认关（讲师演示台可开） |
| 评分复盘 | 关卡记录列表 + 筛选 + 复盘正文（8 维雷达、轨迹、错误四要素、建议、对话回放）+ 摘要 |
| 成长档案 / 知识课堂 / 组长工作台 / 教练编辑器 | 保留，数据源与跳转均改为关卡 |
| 讲师演示台 | 底座右下角一处：演示主线 / 触发红线 / 一键跑完 / 地刀三态 / 雨淋阀异常 / 朗读语音 / 状态清单 / 边界表 / 清空 |

本轮六次口径全部记在 `docs/02_已定决策.md` 第 44–49 条，逐条验收在 `docs/03_任务清单.md`。用户原话要点：陪练本质是学员先做、对则鼓励错则提示；少点选、多说话多操作；不是每一步都适合在屏幕上操作，改做题 + 操作；配音口型对不上先关；陪练舱整个删掉；关卡只留操作相关图示。

## 3. 本会话提交序列（都在分支上）

| 提交 | 内容 |
|---|---|
| e81b19c / 67590a2 | 客户材料解读；8 维能力、关卡引擎、1163 与雨淋阀、组长工作台、偏人力资源首页 |
| c3727d6 | 关卡改教练循环（看 / 做 / 说 + 逐级提示 + 红线制止） |
| 45d559c | 朗读语音恢复；陪练舱全过程去点选 |
| f15304e | 关卡改做题 + 操作；朗读默认关 |
| 2ebaba4 | 修卡死根因（被打断台词不触发回调）；麦克风本地文件可用 |
| aacb34e | 删除陪练舱模块，底座改基于关卡记录 |
| 931f207 | 关卡图示精简；做题不显示场景图；文档重写 |

## 4. 待办（按优先级）

1. **答辩前**：在联网 Chrome / Edge 上完整走一遍两份关卡（真实语速），重点看教练说话被打断、做题作答、按住 / 拖动三处；本地文件打开时麦克风走兜底识别（按当前应答内容打入），联网 http 打开才是真实识别。
2. 题目、分值与 1163 评分表待安全专家审定（入口页显示"待审定"，讲师演示台状态清单据实标注）。
3. HeyGen 片段到货后接入关卡教练 `coachSay`（原播放层已随陪练舱移出，接法见 CLAUDE.md「数字人（HeyGen）现状」）。
4. 首页 / 复盘里的 7 条模拟记录是脱敏编造（`peilian/homedata.js` `HIST_SEED`）；若只要真实记录，把它清空即可，其余页面自动适应。
5. `peilian/style.css` 仍含已删模块的部分样式（不影响功能，只是体积）；有空可清。

## 5. 不要碰的东西

- 铁律六条见 CLAUDE.md（无讲解员式文字、人员数据脱敏且由人确认、不空泛、用词禁区、单文件离线、不推翻已定决策）。验收 grep：`grep -c 演示 dist/*.html` 只许讲师演示台命中，`grep -c 陪练舱 dist/*.html` 为 0。
- 依据条款只引用 `data.js` STEPS / RISKS 既有原文与客户方案场景号，不另编条款号。
- 一次接线图与间隔名用客户真实站名（鲘元Ⅰ线 1891 等），人物全部虚拟。

## 追加 · 项目二 9/21 客户修改已完成

- 提交见 git log「项目二 9/21 客户修改」；口径 docs/02 第 50 条；规格 banzu/README.md；回归 `cd banzu && python3 build.py && node smoke.cjs`（90 项，FAILS 0 ERR none）。
- 新文件：banzu/data5.js（管理数据）、banzu/p_mgmt.js（班组长六页）、banzu/p_mgr.js（管理者八页）；改动：app.js（菜单 / 角色 / 状态清单 / 路由解码）、p_know.js（三类）、style.css（简约覆盖层，文件末尾）、charts.js / charts2.js（主色）、build.py（拼接顺序）、intent.js（管理意图）、smoke.cjs（重写）。
- 待客户确认：星级评价非台账维度的初步得分、试验班与配电运维一班的名册 / 荣誉 / 稳定性底数、年度指标 13 项的目标值与摸高值。
