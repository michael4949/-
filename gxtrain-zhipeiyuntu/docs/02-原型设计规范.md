# 02 原型设计规范

## 命名

- 平台名：**智培云图**
- 副题：南方电网广西电网有限责任公司 · 人才培养数智平台
- 标语：人才可视 · 经验可传承 · 能力可度量

## 配色（`src/index.css` 的 CSS 变量）

| 变量 | 值 | 用途 |
|---|---|---|
| `--ink` | `#14213d` | 正文与主标题 |
| `--indigo` | `#1e3a6e` | 主色，菜单条、表头、主按钮 |
| `--indigo-2` | `#2b4e92` | 次级蓝，图表主色 |
| `--indigo-soft` | `#eaeff7` | 选中行底色 |
| `--gold` | `#a8823a` | 强调色，标题竖条、金线、重点数字 |
| `--gold-2` | `#d4af63` | 浅金 |
| `--paper` | `#fbfbf9` | 工作区底色 |
| `--line` `--line-2` | `#dde3ec` `#eef1f6` | 细线 |
| `--ok` `--warn` `--bad` | `#1d7a4f` `#b8791d` `#b03a2e` | 状态色 |

另有南网色：`CSG_GREEN #00a651`、`CSG_BLUE #0b63b0`（`src/deco.tsx`）。

风格约束：浅色政务风，白底、直角或 2px 小圆角、细边框、无渐变滥用、无紫色。
中文字体栈 PingFang SC / Noto Sans SC / Microsoft YaHei；标题用 `.serif`（宋体系）；数字用 `.num`（等宽数字）。

## 页面结构

```
┌ 标识栏 54px ── 标识 · 平台名 · 全称 | 搜索框 · 日期时钟 · 用户
├ 横版菜单 44px ── 云图首页 + 六个板块（hover 展开二级下拉）
├ 模块标题栏 44px ── 板块名 · 功能切换按钮 · 面包屑 · 返回首页
├ flowband 2px ── 持续流动的金色数据带
├ 工作区 flex-1 ── 各模块内容
└ 主题动效带 126px ── scenes.tsx 对应场景
```

首页无模块标题栏与动效带，整屏是 Hero。

## 背景照片

`src/data.ts` 顶部：

```ts
PHOTO      = 'https://commons.wikimedia.org/wiki/Special:FilePath/Nanning%20Wuxiang%20New%20Area.jpg?width=2200'
PHOTO_ALT  = 'https://i2.chinanews.com.cn/...（广西电网 220kV 盘岭送变电工程实景）'
```

`Hero.tsx` 三级兜底：PHOTO → PHOTO_ALT → 内置 `CityArt()` 实景插画（邕江两岸天际线、喀斯特峰林、滨江绿带、斜拉桥、江面行船、跨江输电通道）。

照片处理方式（不可改成整体压白）：
- 照片占下方 78%，`objectPosition: center 55%`
- 纵向渐变：顶部 0–25% 融入天空，中段 36–62% 完全透明保持照片本色，底部 78–100% 压深
- 标题区单独一层径向柔光
- 卡片用 `rgba(255,255,255,.9)` + `backdrop-filter: blur(4px)`

若客户提供自有高清图，只替换 `PHOTO` 即可，版式不动。

## 元素库（`src/deco.tsx` + `src/scenes.tsx`）

**南网**：`GridMark`（六边形电网标识）、`Tower` / `PylonS`（输电铁塔）、`PowerCorridor`（输电走廊，导线垂弧 + 电流光点）、一次接线画法（母线 / 刀闸触头 / 断路器 / 接地刀闸）

**广西**：`GuangxiMap`（轮廓 + 14 地市局按真实方位 + 骨干通道流动 + 南宁金色扩散环）、`BronzeDrum`（铜鼓太阳纹，12 芒纹 + 联珠圈，慢转 + 外环脉冲）、`BrocadeStrip` / `BrocadeLine` / `.brocade`（壮锦菱形纹）、`Karst`（喀斯特峰林）

**人力资源**：`TalentLadder`（五级技能等级柱 + 人形）、成长阶梯与登阶人形（MapScene）、`Person`、组织树、技能等级证书

**AI**：`AIBrain`（点阵大脑 + 神经连线流动）、`NeuralNet` / `NeuralBlock`（四层网络 4-6-6-3，脉冲逐波前传）

## 动效体系

### 全局（`index.css`，对所有模块自动生效）

| 类 / 选择器 | 效果 |
|---|---|
| `.panel` | 面板升起入场，`:nth-child` 错峰 0.04s |
| `.panel-head::after` | 标题下金线从左展开 |
| `table.grid tbody tr` | 表格逐行左滑入，按行序递延 0.02s（覆盖前 20 行） |
| `.bar-grow` | 条形图 / 进度条 `scaleX` 从 0 生长（`Bars`、`Progress` 已内置） |
| `.pop-in` | 环形图分片、雷达多边形浮现 |
| `.cell-in` | 能力矩阵格子波浪浮现 |
| `.flowband` | 模块标题栏下的流动金色数据带 |
| `.soft-pulse` `.an-glow` `.ring-out` `.flow-line` `.sheen` | 呼吸、扩散环、虚线流动、光泽扫过 |

### 模块专属

- **陪练一次接线图**：母线与带电段红色电流光点持续流动；开关合闸时本体呼吸；地刀合上后扩散脉冲；状态牌（运行/热备用/冷备用/检修）微呼吸；数字人当前发言者金色扩散环 + 口型动画
- **课程工厂**：六步生成进度逐步点亮 + 打字光标
- **问数助手**：脉冲检索提示 + 流式出字
- **成长地图**：雷达由中心展开、六顶点交替放大；热力格波浪浮现（`.cell-in`）；折线描边生长（`dash-draw`）与预测带；散点弹入；干预模拟器滑杆实时重算
- **计划引擎**：四步管线点亮 + 五步 AI 排布逐条完成 + 结果甘特条生长（`.bar-grow`）；建议卡采纳后预测曲线即时重画
- **课程体系**：主干项目卡、期次表与开班日历进度条，与工厂其余功能共用一套动效

### 底部主题动效带（`scenes.tsx`，已拆除，仅保留代码）

| 模块 | 内容 |
|---|---|
| hub | 规程/步骤/案例/诀窍四路汇聚 → 六边形中枢（外圈扩散）→ 课件/题库/陪练剧本/助手回答四路分发 |
| factory | 传送带斜纹推进 + 三组齿轮旋转 + 原料方块流转成课件与题卡 + 神经网络 |
| coach | 两基铁塔进线 → 110kV 母线 → 1163 间隔（刀闸-开关-刀闸-地刀）电流流动；数字人教练 → 学员 → 操作票 |
| ask | 三类角色提问 → 四层神经网络逐波传播 → 条款卡 + 柱状图 |
| map | 五级台阶 + 登阶人形 + 技能等级证书 + 岗位能力雷达 + 三层组织树 |
| plan | 在册员工 → 能力缺口六边形 → 计划生成 → 课程/陪练/集训/认定四通道 |

每条带两端固定有铜鼓太阳纹、喀斯特峰林、顶部壮锦纹样条。

## 陪练舱数据结构（改动最频繁的部分）

剧本在 `src/coach/script.ts`，由客户提供的参考原型剧本替换为广西口径后生成：

```ts
STEPS: Step[]   // 30 步，每步 { no, phase, loc, act, target?, ticket, call, recite, order?, report?, rule, why, redline?, trap?, vd? }
                //   loc 取 phone/wufang/hmi/bay/p8/p20/cab 七个作业位置；act 为 tap/hold/select 等交互动作
                //   ticket 为票面文字，call 为唱票，recite 为复诵，rule/why 为规程依据与原因
                //   redline 标记红线项（带电合地刀 / 异常未中止），trap 标记票令陷阱，vd 标记验电项
RISKS           // 12 条风险辨识 [名称, 措施, 后果]
WUFANG          // 五防模拟 4 项
KNOW            // 知识地图 9 个主题
PREVIEW         // 三个阶段的预习
STEPKP          // 每步知识点卡
```

人物、教练目录、计划、维度与看板数据在 `src/coach/data.ts`：`CHARACTERS`（黄志远 监护人 / 韦岚 南宁地调 / 覃建国 值班负责人）、`COACHES`（38 个教练，`unit` 字段覆盖全部单位组，`avatar` 复用 21 幅形象）、`PLANS`（完整票 / 三个分段 / 三个专项 / 错题重练）、`DIMS6` / `DIMS10`、`TEAM`、`COMPANY_BOARD`（28 个单位）。

引擎在 `src/coach/engine.ts`：模块级状态 `S` + `bump()` 通知，界面用 `useSyncExternalStore` 订阅；`speak()` 返回按字数计时的 Promise，驱动数字人姿态与字幕；阶段 fill → prep → wufang → run → end；五拍 唱票 / 手指口述 / 对，执行 / 执行 / 检查回报；`openReport()` 写入 localStorage（键 `zpyt_*`）。

## 视觉体系（2026 版）

- 令牌在 `src/index.css`：`--indigo` 靛蓝主色，`--ai` / `--ai-2` / `--violet` AI 强调，`--gold` / `--gold-2` 金色重点，`--glass` 玻璃面板底色，`--r` 圆角 14px，`--shadow` 柔和投影
- 页面底：多层径向渐变 + `src/Ambient.tsx` 背景动效层（光晕漂移、微粒缓升、呼吸圆环、掠过光带），首页在实景照片之上叠加同一层
- 面板 `.panel` 玻璃质感；按钮 `.btn` / `.btn-primary`（靛蓝到电光蓝渐变）/ `.btn-gold`；标签 `.tag` 胶囊；分段控件 `.seg`；关键数字 `.num-grad` / `.gold-grad`；AI 元素 `.ai-badge` / `.glow-ring` / `.shimmer`
- 导航：标识栏白色玻璃，菜单栏深靛蓝渐变，当前项为发光胶囊；模块标题栏用 `.seg` 分段胶囊切换功能
- 图表按容器真实像素绘制（`useWidth`），文字不随容器缩放变形

## 各模块的分级页面

分级壳在 `src/drill.tsx`（`DrillShell` / `useDrill` / `jumpTo`），中枢、课程工厂、问数助手、成长地图与千人千面各自在 `nav.tsx` 定义 `Route` 联合类型、`ROOT_OF` 与 `labelOf`。以中枢为例：`Route` 联合类型定义全部页面，`ROOT_OF` 把顶部二级功能映射到一级页，`TAB_OF` 反查所属功能，`labelOf` 生成面包屑文字。`HubShell` 维护栈（push / back / goIndex / replace），页面切换用 `.hub-page` 的进入 / 返回两种过渡；面包屑右侧的三格进度显示当前层级。跨模块跳转用 `window.dispatchEvent(new CustomEvent('app:go', { detail: { node, tab } }))`，`App.tsx` 监听后切换页面。

数据在 `src/hub/data.ts`：`CORE_IN` 手工精选条目（含职能部门与直属机构），`genFromUnits()` 由 `units.ts` 的 180 个场景批量生成条款 / 步骤 / 诀窍 / 题目 / 案例；每条自动带引用关系、版本历史、质量评分。规程版本库 `DOCS`、入库批次 `BATCHES`、专家 `EXPERTS_ALL`、质量分类 `Q_CATS`、权限矩阵 `PERM`、检索意图 `parseIntent()` / `searchAssets()`。

课程工厂的数据在 `src/factory/data.ts`：`CORE_IN` 手工精选课程（生产与职能单位各有代表），`genCourses()` 由评估表 180 个场景生成课程，`QUESTIONS` 由中枢条目生成并逐题挂锚点，`buildDraft()` 按单位 / 岗位 / 课时 / 原料构建草稿，`lectureText()` / `slidesFor()` / `taskbookText()` 生成讲义、课件页与任务书；运行期草稿、试卷与新增课程放在 `store.tsx`。

## 问数助手的问答引擎

`src/ask/engine.ts` 的 `answer(q, roleId, ctx)` 按顺序判断：预置问答（`ASK_BANK`，按身份）→ 动作意图（含"帮我 / 给 / 下发 / 生成 / 预约 / 提醒 / 导出"等动词并匹配技能）→ 指标问答（`findMetric` 命中指标名或别名；维度由"各单位 / 各地市局 / 按班组 / 谁还没 / 趋势"等词与身份范围决定；无指标但为追问句式时沿用 `ctx.metric`）→ 知识问答（中枢 `searchAssets`，按身份过滤敏感条目）→ 未命中（记入缺口）。每类回答都生成 `think` 步骤、`confidence`、`scope`，数据类附 `chart` / `drill` / `caliber`，知识类附 `refs` / `retrieved`，动作类附 `exec`。
指标值由 `metricValue(m, key)` 按指标基线、单位规模与评估表场景均分推导，同一问题重复提问得到同一答案。

## 共用图表（`src/charts.tsx`）

`LineChart`（多序列、预测带 `band`、目标线 `target`、面积与虚线）、`Heatmap`（行列 × 数值，`colorOf` 与 `onCell`）、`Scatter`（九宫格 `quadrants` + `quadLabels`，气泡半径与分组色）、`Pyramid`、`Flow`（分列节点与流量带）、`Columns`、`Ring`、`Gantt`（行 × 跨度，`now` 为当前线）、`Sparkline`。全部按容器真实像素绘制（`useWidth`），文字不随容器缩放变形。`ui.tsx` 的 `Donut` 支持 `label` 与 `center` 两个文字参数。

## 成长地图与千人千面的数据

`src/atlas/data.ts`：`ABILITY_SETS` 九条专业线各六项能力，`GRADE_LINE` 各等级要求线；`buildPerson()` 由姓名、单位、班组、岗位推导人员画像（能力值、12 个月历史、证据记录、潜力、进度、预计达标月数），`teamPersons()` 生成班组成员，`personById()` 反查；`postOfTeam()` 由班组名推断岗位；14 个地市局作为「地市供电局」单位的下一层（`teamsOf` / `bureauReady` / `bureauRadar` / `teamStat`），班组名用「地市局 · 班组」两段式；`POST_MODELS` 176 个岗位模型；`GAPS` / `ALERTS` / `TALENTS` / `KEY_POSTS` / `COHORTS` / `READY_TREND` / `READY_FORECAST` / `FEATURES` / `simulate()` / `CORR`。

`src/plan/data.ts`：`personPlan(p)` 按人员缺口生成计划条目（课程 → 两周内陪练、年度考试、单位重点、个人意向、等级认定），韦明的条目对应 `data.ts` 的 `IDP_ITEMS`；`UNIT_PLANS` / `BUREAU_PLANS` 单位与地市局的学时、完成率、预算、月度计划与实际；`engineStats()` 按权重与范围测算；`DEMANDS` 需求汇总（按单位组与地市局分布）；`CLASSES` / `CONFLICTS` / `RESOLVE` 班次与冲突方案；`BUDGET_CATS` / `budgetLines()`；`FUNNEL` / `CUM_PLAN` / `CUM_ACT` / `overdueOf()`；`optimize()` / `SUGGESTIONS` / `LOAD_PTS`。全部数值稳定可复现，同一输入得到同一结果。
