# 奇点达「3+2+1」AI 工具 · 两件套

两个**纯前端、单文件 HTML** 智能体，内嵌 Claude 大模型（`claude-opus-4-8`）+ 实时联网（`web_search`）。双击即用、无需服务器、无需安装。

| 文件 | 咨询方法（6 步） | 视觉身份 |
|---|---|---|
| `企业高价值AI场景识别与排序.html` | 战略锚定 → 价值链画像 → 痛点与机会诊断 → AI 机会画布 → 多维评估排序 → 路线图与决策；输出**评分矩阵 + 四象限 + 分波次落地路线图** | **深空靛蓝 + 电光紫 + 霓虹青**（未来 AI 气质，神经网络元素） |
| `企业AI投入ROI测算器.html` | 立项与范围界定 → 现状基线量化 → 价值杠杆建模 → 三年 TCO → 风险与情景 → 财务评估与决策；输出含 **ROI / NPV / IRR / 回本 / 敏感性** 的投资决策报告 | **墨黑 + 翡翠绿 + 鎏金**（财务回报气质，刻意差异化） |

> 两者都按**真实企业咨询流程**设计（非问卷）：每步先讲清「为什么问」、用假设驱动的选择题推进、阶段末做「回放 + 洞察」、并适时引入行业基准。
> 推荐流程：先用「场景识别与排序」选出最该投的场景，再用「ROI 测算器」对 TOP 场景精算。

## 怎么用

1. 浏览器打开任一 HTML（推荐 Chrome / Edge）。
2. 首屏填入 **Anthropic API Key**（`sk-ant-...`，仅存浏览器本地 `localStorage`，不上传任何服务器；两个工具共用一次输入）。
3. 跟着问题走——**能选就点选项，选不到再在输入框补充**；跑偏时智能体会主动把话题拉回。
4. 全部回答完，自动生成报告 → 点 **「下载高清 PDF」**。封面为按贵司行业上色的设计型 AI 封面；如需也可点「更换封面」换成自己的图/网址。

## 关键设计

- **顾问式强引导**：像真顾问一样——每步先讲清「为什么问这个」、用假设驱动的**选择题**推进（实在没法选才填空）、阶段末做「回放 + 洞察」、适时引入行业基准；客户跑偏会被温和纠偏。
- **报告 = PDF，排版高端**：A4 多页、Noto Serif 衬线大标题、指标卡 / 评分条 / 结论横幅 / 表格。
- **按行业属性配色**：ROI 报告整体配色随客户行业切换（制造 / 医疗 / 金融…16+ 行业各一套调色板）。
- **设计型 AI 封面（永远高清·有质感·含 AI 元素）**：封面是**矢量生成**的 AI 艺术——渐变光晕 + 神经网络星座 + 数据流 + 网格 + 颗粒噪点；ROI 随行业上色、场景为电光紫×霓虹青。矢量绘制不依赖外部图片，永远清晰、可干净导出 PDF；也可一键换成自己的图。
- **实时联网**：需要最新行业基准 / 报价 / AI 案例时，模型自动调用 `web_search` 核实（可在首屏开关）。
- **稳健兜底**：联网工具不可用时自动降级重试；报告被截断时仍可成稿。

## 安全提示

- **请勿把 API Key 写死进 HTML 后提交到公开仓库**——当前设计是让使用者在页面里自填，密钥只留在其本地浏览器，这是最安全的方式。
- 报告数据含估算成分，正式决策前请由专家团队复核。

---
SINGULARITY REACH TECHNOLOGY · 奇点达科技
