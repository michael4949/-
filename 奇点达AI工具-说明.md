# 奇点达「3+2+1」AI 工具 · 两件套

两个**纯前端、单文件 HTML** 智能体，内嵌 Claude 大模型（`claude-opus-4-8`）+ 实时联网（`web_search`）。双击即用、无需服务器、无需安装。

| 文件 | 作用 | 视觉身份 |
|---|---|---|
| `企业高价值AI场景识别与排序.html` | 沿业务链扫描痛点 → 生成 AI 场景 → 按「价值×可行性」排序，输出**带评分矩阵、四象限、落地路线图**的报告 | **深空靛蓝 + 电光紫 + 霓虹青**（未来 AI 气质，含神经网络元素） |
| `企业AI投入ROI测算器.html` | 把「感觉能省钱」拆成精确财务数字 → 输出含 **ROI / NPV / IRR / 回本周期 / 敏感性分析**的投资决策报告 | **墨黑 + 翡翠绿 + 鎏金**（财务回报气质，与上者刻意差异化） |

> 推荐流程：先用「场景识别与排序」选出最该投的场景，再用「ROI 测算器」对 TOP 场景精算。

## 怎么用

1. 浏览器打开任一 HTML（推荐 Chrome / Edge）。
2. 首屏填入 **Anthropic API Key**（`sk-ant-...`，仅存浏览器本地 `localStorage`，不上传任何服务器；两个工具共用一次输入）。
3. 跟着问题走——**能选就点选项，选不到再在输入框补充**；跑偏时智能体会主动把话题拉回。
4. 全部回答完，自动生成报告 → 点 **「下载高清 PDF」**。封面图按贵司行业自动联网匹配，可点「更换封面」换成自己的图/网址。

## 关键设计

- **强引导 · 选择题优先**：每轮只问一件事，尽量给选项，实在没法选才填空；答偏了会被温和纠偏。
- **报告 = PDF，排版高端**：A4 多页、Noto Serif 衬线大标题、指标卡 / 评分条 / 结论横幅 / 表格。
- **按行业属性配色 + 定制封面**：ROI 报告整体配色随客户行业切换（制造/医疗/金融…16+ 行业）；两个工具的封面都按行业关键词**联网检索**一张高相关图片（loremflickr，带 CORS 头，可干净导出 PDF），并叠加品牌色蒙版，呈现「为贵司定制」之感。
- **实时联网**：需要最新行业基准 / 报价 / AI 案例时，模型自动调用 `web_search` 核实（可在首屏开关）。
- **稳健兜底**：联网工具不可用时自动降级重试；封面图跨域失败时回退品牌渐变；报告被截断时仍可成稿。

## 安全提示

- **请勿把 API Key 写死进 HTML 后提交到公开仓库**——当前设计是让使用者在页面里自填，密钥只留在其本地浏览器，这是最安全的方式。
- 报告数据含估算成分，正式决策前请由专家团队复核。

---
SINGULARITY REACH TECHNOLOGY · 奇点达科技
